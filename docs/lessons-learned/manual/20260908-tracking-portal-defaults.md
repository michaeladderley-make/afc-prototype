# Tracking IDs stay page-local unless edited on Profile

- Portal tracking defaults live on Profile. Publish may override them for that page only. Do not copy page IDs into Portal defaults on publish.
- Blank pixel fields are valid. Invalid values (not `G-…`, numeric Meta/UET, or `AW-…`) show inline field errors and still save.
