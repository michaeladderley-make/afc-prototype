# create-next-app folder names

- `create-next-app` uses the directory name as the npm package name. Folders with spaces or capitals (for example `AFC Design`) fail npm naming rules.
- Scaffold into a kebab-case subdirectory (`afc-design`), then move the files up if the workspace folder must keep its original name.
